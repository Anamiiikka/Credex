import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    default: "Credex — AI Spend Auditor",
    template: "%s | Credex",
  },
  description:
    "Find out how much your team is overpaying for AI tools. Get a free audit of your ChatGPT, Claude, Cursor, and GitHub Copilot spend in under 2 minutes.",
  metadataBase: new URL("https://credex.rocks"),
  openGraph: {
    siteName: "Credex",
    type: "website",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
  },
  twitter: { card: "summary_large_image" },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html
      lang="en"
      /* dark activates the .dark CSS-var overrides and shadcn dark variants */
      className={`${geistSans.variable} ${geistMono.variable} dark h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
